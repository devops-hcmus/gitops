apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: dev-payment-paypal
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/devops-hcmus/yas-cd
    targetRevision: main
    path: k8s/charts/payment-paypal
    helm:
      valueFiles:
        - values.yaml
        - https://raw.githubusercontent.com/devops-hcmus/gitops/main/clusters/dev/payment-paypal/values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: dev
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
