apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: dev-payment-paypal
  namespace: argocd
spec:
  project: default
  sources:
    - repoURL: https://github.com/devops-hcmus/yas-cd
      targetRevision: main
      path: k8s/charts/payment-paypal
      helm:
        valueFiles:
          - values.yaml
          - $values/clusters/dev/payment-paypal/values.yaml
    - repoURL: https://github.com/devops-hcmus/gitops
      targetRevision: main
      ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: dev
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
